apiVersion: v1
kind: Service
metadata:
  name: {{ include "wcfservice.fullname" . }}
  labels:
    {{- include "wcfservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wcfservice.selectorLabels" . | nindent 4 }}
