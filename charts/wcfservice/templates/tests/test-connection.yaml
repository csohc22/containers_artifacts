apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wcfservice.fullname" . }}-test-connection"
  labels:
    {{- include "wcfservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wcfservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
